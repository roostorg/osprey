{
    "sql": "SELECT * FROM (SELECT *, LAG(__time, 9) OVER (ORDER BY __time) AS pt1 FROM \"datasource\" WHERE Endpoint = '/foo') AS __inner WHERE pt1 IS NULL OR TIMESTAMPDIFF(SECOND, pt1, __time) > 600",
    "type": "sql"
}