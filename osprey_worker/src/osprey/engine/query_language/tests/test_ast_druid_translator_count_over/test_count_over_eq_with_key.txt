{
    "sql": "SELECT * FROM (SELECT *, LAG(__time, 9) OVER (PARTITION BY UserId ORDER BY __time) AS pt1, LAG(__time, 10) OVER (PARTITION BY UserId ORDER BY __time) AS pt2 FROM \"datasource\" WHERE (UserLoginIp = '1.1.1.1' AND Endpoint = '/foo')) AS __inner WHERE pt1 IS NOT NULL AND TIMESTAMPDIFF(SECOND, pt1, __time) <= 600 AND (pt2 IS NULL OR TIMESTAMPDIFF(SECOND, pt2, __time) > 600)",
    "type": "sql"
}