{
    "sql": "SELECT * FROM (SELECT *, LAG(__time, 4) OVER (PARTITION BY UserId ORDER BY __time) AS pt1, LAG(__time, 5) OVER (PARTITION BY UserId ORDER BY __time) AS pt2 FROM \"datasource\" WHERE Endpoint = '/foo') AS __inner WHERE NOT (pt1 IS NOT NULL AND TIMESTAMPDIFF(SECOND, pt1, __time) <= 600 AND (pt2 IS NULL OR TIMESTAMPDIFF(SECOND, pt2, __time) > 600))",
    "type": "sql"
}