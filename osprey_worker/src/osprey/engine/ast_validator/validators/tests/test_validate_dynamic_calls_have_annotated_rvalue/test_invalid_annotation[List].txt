error: can only check dynamic return values of simple types or `Entity`, `ExtractLiteral`, `ExtractSecret`, `List`, `Optional`, `Secret`, `Union` generics
--> main.sml:1:5
     |
   1 | Foo: List = JsonData(path='$.foo')
     |      ^ `List` is generic, try `List[T]` where `T` is a type like `str` or `int`