# TEMP DIAGNOSTIC override (do not merge): grant SYS_PTRACE so py-spy (run via
# `docker exec` after the hang has set in) can attach to the hung PID 1.
services:
  test_runner:
    cap_add:
      - SYS_PTRACE
