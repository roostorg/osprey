[project]
name = "osprey-rpc"
version = "0.1.0"
description = "RPC/protobuf pythong bindings for Osprey"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "grpcio>=1.82.1",
    "protobuf>=7.35,<8",
]
